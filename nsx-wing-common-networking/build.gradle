description = 'The shared networking components of the NSXWing Client & Server'

dependencies {
	runtime(
			[group: 'ch.qos.logback', name: 'logback-classic',  version: project.logbackVersion],
	)
}
